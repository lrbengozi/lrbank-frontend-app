import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mainTextInput: {
    marginTop: 15,
    borderRadius: 10,
    padding: 10,
    borderColor: '#e46400',
    borderWidth: 1,
  },
});

export default styles;
