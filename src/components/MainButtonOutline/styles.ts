import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  appMainButtonOutline: {
    marginTop: 10,
    borderRadius: 10,
    padding: 10,
    borderColor: '#e46400',
    borderWidth: 1,
  },
  appMainButtonOutlineText: {
    fontSize: 16,
    color: '#e46400',
    textAlign: 'center',
  },
});

export default styles;
